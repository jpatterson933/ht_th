<section class = "prof">
  <div class="container">
    <div class="main-body">
      {{!-- Profile section only display username and email --}}
      <div>
        <h4>{{username}}</h4>
        {{email}}
      </div>
    </div>
  </div>
</section>
      <div>
        <h1>Your characters</h1>
        {{> character}}
      </div>